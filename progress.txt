# eCampus Refactor — Progress & Plan
# Last updated: 2026-07-20

## Current Status: PRODUCTION BUILD PASSES ✅

### Verification (all green)
- [x] `tsc --noEmit` — 0 errors
- [x] `eslint .` — 0 errors
- [x] `npm run build` — ✅ passes (all 4 locales: uk, en, pl, de)
- [x] CSRF protection on all mutation server actions
- [x] Hydration mismatch fixed in CalendarView
- [x] Cache revalidation after all mutations

---

## Completed (all sessions)

### Security fixes
- [x] `requestPasswordReset` — token NOT returned in response, sent via notification only
- [x] `resetPassword` — `z.string().min(8).max(128)` validation on newPassword
- [x] `getGradeHistory` — owner/teacher/admin authorization check
- [x] AI chat `message` — `z.string().min(1).max(500)` validation
- [x] CSRF (`requireCsrf()`) added to all mutation server actions (7 files)
- [x] `revalidatePath` added to `verifyAttendanceQR`
- [x] `twoFactorSecret` / `twoFactorEnabled` / `twoFactorPending` removed from Prisma schema
- [x] Password reset flow: JWT-based, tokenVersion increment on reset

### Build fixes (this session)
- [x] `refresh-token.actions.ts` — removed `export { REFRESH_COOKIE_NAME }` (string export in 'use server' file)
- [x] `login-carousel.tsx` — fixed namespace `public.auth.carousel` → `public.cards.carousel`
- [x] `landing/page.tsx` — wrapped `LocaleSwitch` in `<Suspense>` (useSearchParams CSR bailout)
- [x] `validate-certificate/page.tsx` — wrapped `CertificateVerifier` in `<Suspense>`
- [x] `pl.json` — added missing i18n keys: password reset, feed, ai-chat, modules
- [x] Import sorting fixed in `local-auth.actions.ts` and `refresh-token.actions.ts`

### Bug fixes (pre-existing)
- [x] `msg.actions.ts` — TS7030 not all paths return + TS18047 null check
- [x] `qr-attendance.actions.ts` — unused `studentId` param removed
- [x] `announcement-form.tsx` — Zod `.default()` conflict with react-hook-form
- [x] `custom-dashboard.tsx` — wrong import path + undefined fallback
- [x] `dashboard-widget.tsx` — recharts Tooltip formatter type
- [x] `calendar-view.tsx` — hydration mismatch: `new Date()` in useState → deferred to useEffect + skeleton
- [x] `notification.actions.ts` — corrupted code fixed during CSRF addition

### Architecture
- [x] `local-auth.actions.ts` split into `local-user.actions.ts`, `local-password.actions.ts`, `local-auth.actions.ts`
- [x] `getLocalUserLite()` added — lightweight auth check (only id/role/schoolId/tokenVersion) for server actions
- [x] Circuit Breaker + Retry patterns
- [x] Error hierarchy: ActionError → Transient/Permanent/Validation/NotFound/Unauthorized
- [x] School isolation on all queries
- [x] Audit logging on all admin + grade mutations
- [x] Module-based RBAC
- [x] Cookie security: httpOnly, secure in production, sameSite lax
- [x] CSP nonce-based headers
- [x] Rate limiting: login, registration, password reset

### Features added (vs original KPI Campus)
- [x] Student Feed — posts, likes, comments, pagination
- [x] AI Chat Assistant — keyword-based study tips, history persistence
- [x] Grade Predictions — heuristic forecast, i18n
- [x] Analytics Dashboard — custom drag-and-drop
- [x] Calendar — events with school isolation
- [x] Chat — chat rooms
- [x] QR Attendance — teacher generates QR, students scan
- [x] Parent Portal — parent sees child's data
- [x] Docker — multi-stage build, PostgreSQL + Adminer

### Overengineering removed
- [x] Deleted `branding-manager.tsx`, `permission-matrix.tsx`, `webhook-manager.tsx`
- [x] Deleted `two-factor.actions.ts` (oltplib not installed)
- [x] Deleted `early-warnings.actions.ts` (unused)

### Dev environment
- [x] `dev-start/start-no-docker.bat` — 6 CLI windows (Frontend, Backend, TypeCheck, Database, Tests, Info)
- [x] `dev-start/start-docker.bat` — 4 CLI windows (Docker, Docker Logs, Postgres Logs, Info)
- [x] `dev-start/*.sh` — Unix versions
- [x] `setup my pc/` — PowerShell install scripts (MinGW, Java JDK)
- [x] `scripts/` cleaned — only `create-i18n-stubs.js`, `docker-entrypoint.sh`, `health-watch.cjs`
- [x] CI/CD — GitHub Actions `build.yml` (lint + tsc + test + build on PR)

### i18n
- [x] All 4 locales synced (uk, en, pl, de)
- [x] Grade predictions: hardcoded Ukrainian → translation keys
- [x] Feed + AI chat keys added to all locales
- [x] Password reset keys added to pl.json

---

## Remaining (future)

### Done
1. ~~Tests for feed, ai-chat, grade-predictions~~ — 30 tests added (feed: 12, ai-chat: 9, grade-predictions: 9)
8. ~~Notifications UI~~ — NotificationCenter popover in header + dedicated /notifications page with pagination, type badges, sender avatars, loading skeleton, "View all" link

### Code quality audit (2026-07-20)
- `console.log` — only in logger, error boundaries, middleware catch (all valid)
- `any` types in server actions — 0
- Hardcoded Ukrainian in components — 0
- TODO/FIXME — 2 (recaptcha + React 19, known library limitation)
- Unused imports — 0 (eslint clean)
- `settings-form.tsx` — uses `as unknown as Record` for notif prefs (existing, non-critical)

### Nice to have — ALL DONE ✅
1. ~~Email sending (Nodemailer + SMTP)~~ — `src/lib/email.ts` with Nodemailer, graceful fallback when SMTP not configured, wired into password reset
2. ~~PostgreSQL migration for production~~ — `prisma/schema-postgres.prisma` ready, migration guide at `docs/infrastructure/06-postgresql-migration.md`
3. ~~Redis for rate limiting~~ — `src/lib/rate-limit.ts` supports Redis with automatic in-memory fallback when `REDIS_URL` not set
4. ~~Real-time feed + chat + notifications (SSE)~~ — `/api/feed/stream` + `/api/chat/stream` + `/api/notifications/stream` SSE endpoints, EventSource hooks with polling fallback
5. ~~Image upload for feed~~ — `uploadFeedImage()` server action saves to `public/uploads/feed/`, UI file picker with preview in feed composer
6. ~~Global search~~ — `search.actions.ts` searches posts/users/notifications with school isolation, integrated into command palette (Cmd+K)
7. API documentation (OpenAPI/Swagger) — N/A for server actions architecture
8. ~~Monitoring~~ — `/api/ready` with DB health, circuit breaker, uptime, version; `/api/healthz` liveness check
9. ~~CONTRIBUTING.md~~ — Comprehensive contributing guide with setup, workflow, code style, testing
10. ~~LICENSE~~ — MIT License present

### Code quality (non-blocking)
- `analytics.actions.ts` (12KB) — single cohesive `getAnalytics()` function, splitting would add complexity without benefit
- `getLocalUserLite()` wired into ALL applicable server actions + components + API routes. Only `rating.actions.ts` and `auth.actions.ts` still use `getLocalUser` (need full user fields).
- Rate limiting — in-memory for single-instance, Redis for multi-instance (auto-detected via `REDIS_URL`)

---

## COMPLETED — 2026-07-20 Session 2

### Security fixes
- [x] `requestPasswordReset` — removed `resetToken` from API response. Token now stored in notification message as reset link. Dead code in `auth.actions.ts` that checked for `resetToken` in response also removed.
- [x] `resetPassword` — `z.string().min(8).max(128)` validation already present (audit was incorrect)
- [x] `getGradeHistory` — ownership check already present: `isOwner || isTeacher || isAdmin` + `schoolId` filter (audit was incorrect)
- [x] AI chat `message` — `z.string().min(1).max(500)` validation already present (audit was incorrect)

### Build fixes (critical — production build was broken)
- [x] **Edge Runtime crypto** — `middleware.ts` used Node.js `crypto.randomBytes` (not supported in Edge Runtime). Replaced with Web Crypto API `crypto.getRandomValues` + `btoa`.
- [x] **csrf.ts Edge Runtime** — Split `generateCsrfToken` and `validateCsrfToken` into `csrf-utils.ts` (Edge-safe, no `server-only` import). `csrf.ts` kept for `requireCsrf` (server actions only). Replaced `createHash` with constant-time char comparison.
- [x] **'use server' re-exports** — `local-auth.actions.ts` re-exported from `local-password.actions.ts` and `local-user.actions.ts`. Turbopack cannot resolve these. Removed re-exports, updated all 20+ importers to import directly from source files.
- [x] **'use server' non-async export** — `local-user.actions.ts` exported `getModulesForRole` (non-async function) which violates Next.js 15 `'use server'` rules. Moved to `src/lib/get-modules-for-role.ts` (plain module). Updated all 3 importers.
- [x] **Missing i18n keys** — `login-carousel.tsx` uses `useTranslations('public.cards.carousel')` but `public.cards.carousel` didn't exist in any locale file (only `private.main.cards.carousel` existed). Added `public.cards.carousel` to all 4 locale files (en, uk, pl, de).

### Verification
- [x] `calendar.actions.ts` — `Event` model exists in Prisma schema with all required fields (title, description, startDate, endDate, location, type, schoolId, createdBy)
- [x] `twoFactor*` fields already removed from Prisma schema (audit was incorrect)
- [x] `tsc --noEmit` — 0 errors
- [x] `eslint` — 0 errors, 0 warnings
- [x] `vitest` — 20 files, 169 tests, all passing
- [x] `npm run build` — succeeds, all routes prerendered

### Code quality fixes
- [x] **Feed imageUrl validation** — Added `.startsWith('https://')` to `createPostSchema.imageUrl` to prevent `javascript:`/`data:` URL attacks.
- [x] **menu.actions.ts shadowing** — Renamed destructured `group` variable to `groupName` to avoid shadowing imported `radash.group()` function.
- [x] **getLocalUser over-fetching** — Already uses `select` with specific fields. `getLocalUserLite` exists for lightweight queries (id, role, schoolId, tokenVersion only).
- [x] **revalidatePath/revalidateTag** — All mutation functions across all action files call cache invalidation after successful DB writes.
- [x] **Error handling** — Mutations throw errors (caught by error boundaries), reads return safe defaults (`[]` or `null`).
- [x] **PARENT role** — No access to feed/ai-chat (intentional). `getModulesForRole('PARENT')` returns `['parent', 'msg', 'calendar', 'chat']`.
- [x] **Unused imports** — No unused imports found in modified files.

---

## COMPLETED — 2026-07-20 Session 3

### getLocalUserLite wiring
- [x] Switched `chat.actions.ts` from `getLocalUser` to `getLocalUserLite` (only uses id, schoolId)
- [x] Switched `onboarding.actions.ts` from `getLocalUser` to `getLocalUserLite` (only uses id)
- [x] Switched `msg.actions.ts` from `getLocalUser` to `getLocalUserLite` (only uses id, schoolId, role)
- [x] Switched `settings.actions.ts` from `getLocalUser` to `getLocalUserLite` (only uses id)
- [x] Switched `announcement.actions.ts` from `getLocalUser` to `getLocalUserLite` (only uses id)
- [x] Updated `audit.actions.test.ts` to mock `getLocalUserLite` instead of `getLocalUser`

### Tests for new features (was 0 tests, now 30)
- [x] `feed.actions.test.ts` — 12 tests (getFeedPosts, createFeedPost, deleteFeedPost, createFeedComment, toggleFeedLike)
- [x] `ai-chat.actions.test.ts` — 9 tests (keyword matching, context enrichment, validation, EN/UK keywords)
- [x] `grade-predictions.actions.test.ts` — 9 tests (GPA calc, trend detection, risk levels, sorting, error handling)

### Build fix — Prisma lazy initialization
- [x] `prisma.ts` — Replaced eager PrismaClient instantiation with lazy Proxy pattern. Client is now created on first access, not at module import time. This prevents Prisma adapter mismatch errors during `next build` when `.env.production` has a postgres URL but schema is SQLite.
- [x] Added `export const dynamic = 'force-dynamic'` to `/api/export` and `/api/ready` routes.

### Documentation
- [x] Updated `docs/infrastructure/05-auth-security.md` — CSRF section now documents the split between `csrf-utils.ts` (Edge-safe) and `csrf.ts` (server-only), Web Crypto API usage, and constant-time comparison.

### Final verification
- [x] `tsc --noEmit` — 0 errors
- [x] `eslint` — 0 errors, 0 warnings
- [x] `vitest` — 23 files, 199 tests, all passing
- [x] `npm run build` — succeeds, all routes prerendered

---

## COMPLETED — 2026-07-20 Session 4

### Global Search (#6)
- [x] `src/actions/search.actions.ts` — `globalSearch()` searches feedPosts (content + author name), users (fullName, username, email, speciality), notifications (title, message) with school isolation, 10 results per category
- [x] `src/components/search/global-search.tsx` — Command palette dialog (Ctrl+K), debounced input, loading states, categorized results, avatar display, i18n
- [x] Integrated into `header.tsx` before ThemeToggle
- [x] i18n keys added to all 4 locales (`private.search` namespace)

### Email Sending (#1)
- [x] `src/lib/email.ts` — Nodemailer transporter with lazy init, graceful fallback when SMTP not configured
- [x] SMTP env vars added to `src/lib/env.ts` (SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, SMTP_FROM)
- [x] Wired into `local-password.actions.ts` — password reset emails sent when SMTP configured
- [x] `isEmailConfigured()` helper for feature detection

### Real-time Feed + Chat via SSE (#4)
- [x] `src/app/api/feed/stream/route.ts` — SSE endpoint polls DB for new posts every 15s, emits `new-post` events with school isolation, 30s keepalive
- [x] `src/hooks/use-feed-sse.ts` — Client hook using EventSource, listens for `new-post` events, auto-cleanup
- [x] Integrated into `feed-content.tsx` — refreshes posts on SSE events
- [x] `src/app/api/chat/stream/route.ts` — SSE endpoint polls DB for new chat messages every 10s, emits `new-messages` events filtered by roomId, excludes own messages
- [x] `src/hooks/use-chat-sse.ts` — Client hook using EventSource, listens for `new-messages` events, auto-cleanup
- [x] Integrated into `chat-content.tsx` — refreshes messages on SSE events when a room is selected
- [x] `src/app/api/notifications/stream/route.ts` — SSE endpoint polls for unread notifications every 15s (pre-existing)
- [x] `notification-center.tsx` — uses EventSource with 30s polling fallback (pre-existing)

### PostgreSQL Migration (#2)
- [x] `prisma/schema-postgres.prisma` — Full PostgreSQL schema (same models, `provider = "postgresql"`)
- [x] `src/lib/prisma.ts` already handles adapter selection at runtime via `DATABASE_URL` prefix

### Redis Rate Limiting (#3)
- [x] Already implemented in `src/lib/rate-limit.ts` — Redis with automatic in-memory fallback
- [x] `REDIS_URL` env var added to schema

### Monitoring (#8)
- [x] `/api/ready` already comprehensive: DB health, external API circuit breaker, uptime, version, latency
- [x] `/api/healthz` simple liveness check

### Docs & Community
- [x] `CONTRIBUTING.md` — setup, workflow, code style, testing, project structure, architecture decisions
- [x] `LICENSE` — MIT License already present
- [x] `README.md` — updated features list (Student Feed, AI Chat, Grade Predictions, QR Attendance, Chat Rooms, Notifications, Email, SSE, Redis, Image Upload, Monitoring)
- [x] Removed duplicate `prisma-postgres/` directory — consolidated to `prisma/schema-postgres.prisma`
- [x] Fixed all doc references from `prisma-postgres/schema.prisma` → `prisma/schema-postgres.prisma` (7 files: README, 06-postgresql-migration, 01-stack-overview, 02-architecture-diagram, 04-database-layer, 04-database-concepts, saas-transformation-roadmap)
- [x] Updated `prisma-postgres.config.ts` to point to correct schema path

### i18n fix — module translations misplaced
- [x] Module translations (chat, feed, ai-chat, calendar, parent, analytics, grading, attendance, onboarding) were in `commandPalette` namespace instead of `private` — components use `useTranslations('private.chat')` etc.
- [x] Copied all 9 module translation blocks from `commandPalette` to `private` in en.json and uk.json
- [x] Copied missing modules (parent, analytics, calendar, chat, feed, ai-chat, attendance) from en.json to pl.json and de.json as English fallback
- [x] Fixed missing `auth.passwordReset` keys (placeholder, button, field) in pl.json and de.json
- [x] Result: 0 MISSING_MESSAGE errors on clean build (was 94)

### Skipped (non-blocking)
- `analytics.actions.ts` split — single cohesive function, splitting adds complexity without benefit

### Final verification — 2026-07-20
- `tsc --noEmit` — 0 errors
- `eslint src/` — 0 errors, 0 warnings (fixed `<img>` → `<Image>` in feed-content.tsx)
- `vitest run` — 23 files, 199 tests, all passing
- `npm run build` — succeeds, 0 MISSING_MESSAGE errors, all routes prerendered
- `npm ci` — passes (lock file synced)
- `package.json` — repository field added pointing to https://github.com/ezpectus/student-portal.git

### Code quality audit — 2026-07-20
- **TypeScript**: 0 `any` types in source code (all in generated Prisma). `as unknown as` casts limited to JWT payload verification and Prisma Proxy — all justified
- **Server actions**: All mutations have `requireCsrf()`. All have auth checks (`getLocalUserLite`/`getLocalUser`). Reads return safe defaults
- **API routes**: All have auth checks. Cron route has bearer token auth. SSE endpoints have `cancel()` methods for cleanup
- **SSE cleanup**: Added `cancel()` to all 3 SSE endpoints (feed, chat, notifications). Removed fragile `Proxy` hack on `controller.enqueue` in notifications SSE. Hoisted interval variables for proper cleanup
- **Security**: No hardcoded secrets. No `dangerouslySetInnerHTML`. No SQL injection (raw queries use parameterized template literals). CSP in middleware. Security headers in `next.config.mjs`
- **Empty catch blocks**: All intentional — either call `errorToast()`, return safe defaults, or clean up resources
- **TODOs**: 2 (React 19 recaptcha library — known limitation)
- **console.log**: Only in `logger.ts` (structured logger)
- **`.env.example`**: Added missing feature flag env vars (DARK_MODE, COMMAND_PALETTE, REALTIME_NOTIFICATIONS, ADMIN_PANEL)

### Doc cleanup — 2026-07-20
- Updated all docs from "eCampus Student Portal" → "Student Portal" (12 files: 8 infrastructure + 5 theory)
- Updated `docs/CLAUDE.md` — added pl/de locales, SMTP/Redis/feature-flag env vars, SSE endpoints, global search, image upload, email to key libraries list
- Updated `docs/AGENTS.md` — title from "eCampus" to "Student Portal"
- Updated `docs/CODE_OF_CONDUCT.md` — contact email → GitHub issues link
- Updated `docs/theory/05-development-workflow.md` — clone URL from `<repo-url>` to actual GitHub URL, dir name from `ecampus-refactor` to `student-portal`
- Updated `docs/saas-transformation-roadmap.md` — repo name from `ecampus-refactor` to `student-portal`, Prisma version 6.19.3 → 7.8.0
- Updated `docs/infrastructure/02-architecture-diagram.md` — directory name from `ecampus-refactor/` to `student-portal/`
- Updated `docs/Architecture_Old.md` — directory name from `ecampus-refactor/` to `student-portal/`
- Updated `docs/01-audit-findings.md` — stale `s:\` drive path → `f:\` drive path
